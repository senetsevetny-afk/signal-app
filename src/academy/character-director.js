/* MARKET AI Academy — Character Director v1.
   Coordinates lesson events, mentor scenes and chart cues without coupling
   lesson content to the final Rive/2.5D/3D renderer. */
(function(){
  const listeners=new Set();
  let scene={phase:'idle',state:'idle',action:'observe',position:'left',speech:null,target:null,chartCue:null,noHint:false};
  function derive(event,payload={}){
    if(event==='lesson:step'){
      const step=payload.step||{};
      if(step.type==='market_replay') return {phase:'replay',state:'thinking',action:'observe_replay',position:'left',speech:'Рынок будет открываться частями. На каждой остановке решай только по тем свечам, которые уже видны.',target:'scenario_chart',chartCue:'replay_wait',noHint:true};
      if(step.type==='drag_level') return {phase:'your_turn',state:'point',action:'step_away_and_watch',position:'left',speech:'Поставь горизонтальный уровень сам. Я покажу эталон только после твоего решения.',target:'scenario_chart',chartCue:'level_search',noHint:true};
      if(step.type==='mark_structure_sequence') return {phase:'your_turn',state:'thinking',action:'watch_ordered_swings',position:'left',speech:'Собери swing-точки по порядку. Я не покажу путь заранее — после последней точки сравним последовательности.',target:'scenario_chart',chartCue:'structure_sequence_search',noHint:true};
      if(step.type==='mark_structure') return {phase:'your_turn',state:'thinking',action:'step_away_and_watch',position:'left',speech:'Прочитай структуру и нажми на нужный swing. Сначала твой анализ — потом мой разбор.',target:'scenario_chart',chartCue:'structure_search',noHint:true};
      if(step.type==='tap_zone') return {phase:'your_turn',state:'point',action:'step_away_and_watch',position:'left',speech:'Теперь отметь зону сам. Я не буду рисовать правильный уровень заранее — проведи пальцем по графику.',target:'scenario_chart',chartCue:'zone_search',noHint:true};
      if(step.type==='tap_candle') return {phase:'your_turn',state:'point',action:'step_away_and_watch',position:'left',speech:'Теперь найди свечу сам. Нажми прямо на график — я не буду показывать ответ заранее.',target:'scenario_chart',chartCue:'tap_search',noHint:true};
      if(step.type==='select_direction') return step.decisionMode==='entry_timing'?{phase:'your_turn',state:'thinking',action:'step_away',position:'left',speech:'Сначала прочитай событие вокруг уровня. Реши: ENTER, WAIT или SKIP. Я покажу разметку только после выбора.',target:'scenario_chart',chartCue:'focus',noHint:true}:{phase:'your_turn',state:'thinking',action:'step_away',position:'left',speech:'Теперь твоя очередь. Я не буду подсказывать направление — оцени график и выбери UP, DOWN или SKIP.',target:'scenario_chart',chartCue:'focus',noHint:true};
      if(['question','true_false','multiple_choice'].includes(step.type)) return {phase:'your_turn',state:'thinking',action:'wait_for_answer',position:'left',speech:'Сначала ответь сам. После выбора я разберу решение вместе с тобой.',target:'answers',chartCue:null,noHint:true};
      return {phase:'teaching',state:step.characterState||'explain',action:'explain',position:'left',speech:null,target:null,chartCue:null,noHint:false};
    }
    if(event==='level:submitted'){const ok=!!payload.correct;return {phase:'review',state:ok?'correct':'incorrect',action:'inspect_level',position:'right',speech:ok?'Уровень внутри рабочей зоны. Теперь сравним его с эталоном.':'Сравним линию с зоной повторных реакций — точность не должна отрываться от контекста.',target:'scenario_chart',chartCue:ok?'level_ok':'level_missed',noHint:false};}
    if(event==='structure_sequence:submitted'){const ok=!!payload.correct;return {phase:'review',state:ok?'correct':'incorrect',action:'trace_structure_path',position:'right',speech:ok?'Да. Последовательность swing-точек читается правильно — теперь я соединю путь.':'Сравни порядок точек с зелёным эталоном. В структуре важна последовательность, а не одна вершина.',target:'scenario_chart',chartCue:ok?'structure_sequence_ok':'structure_sequence_missed',noHint:false};}
    if(event==='structure:submitted'){const ok=!!payload.correct;return {phase:'review',state:ok?'correct':'incorrect',action:'inspect_structure',position:'right',speech:ok?'Да. Это нужная структурная точка. Сравни её с предыдущими swing.':'Посмотри на последовательность максимумов и минимумов. Я отмечу эталон после твоего выбора.',target:'scenario_chart',chartCue:ok?'structure_ok':'structure_missed',noHint:false};}
    if(event==='zone:submitted'){const ok=!!payload.correct;return {phase:'review',state:ok?'correct':'incorrect',action:'compare_zones',position:'right',speech:ok?'Да. Твоя область близка к зоне повторных реакций. Сравним её с эталоном.':'Сравним твою область с повторными минимумами. Зона должна объяснять несколько реакций, а не одну точку.',target:'scenario_chart',chartCue:ok?'zone_ok':'zone_missed',noHint:false};}
    if(event==='candle:submitted'){const ok=!!payload.correct;return {phase:'review',state:ok?'correct':'incorrect',action:'inspect_candle',position:'right',speech:ok?'Да. Ты нашёл нужную свечу — теперь сравни её тело с соседними.':'Я отмечу твой выбор. Сравни размер тела с соседними свечами и найди более сильную.',target:'scenario_chart',chartCue:ok?'candle_ok':'candle_missed',noHint:false};}
    if(event==='replay:play') return {phase:'replay_motion',state:'idle',action:'track_candles',position:'left',speech:'Наблюдаю вместе с тобой. Не пытайся угадать будущую свечу — читай только то, что уже сформировано.',target:'scenario_chart',chartCue:'replay_motion',noHint:true};
    if(event==='replay:pause') return {phase:'replay_pause',state:'thinking',action:'freeze_and_observe',position:'left',speech:'Пауза. Используй её, чтобы заново прочитать структуру, уровень и импульс.',target:'scenario_chart',chartCue:'replay_pause',noHint:true};
    if(event==='replay:tick') return {phase:'replay_motion',state:'idle',action:'follow_latest_candle',position:'left',speech:'',target:'scenario_chart',chartCue:'replay_motion',noHint:true};
    if(event==='replay:gate') return {phase:'replay_gate',state:'thinking',action:'step_away_for_decision',position:'left',speech:'Стоп. Новых свечей не будет, пока ты не примешь решение по текущему контексту.',target:'scenario_chart',chartCue:'replay_gate',noHint:true};
    if(event==='replay:decision'){const ok=!!payload.correct;return {phase:'replay_review',state:ok?'correct':'incorrect',action:'inspect_replay_decision',position:'right',speech:ok?'Хорошо. Зафиксируй, почему это решение подходит именно этому этапу — затем откроем рынок дальше.':'Разберём только доступный сейчас контекст. Будущие свечи не должны оправдывать решение задним числом.',target:'scenario_chart',chartCue:ok?'replay_ok':'replay_missed',noHint:false};}
    if(event==='replay:advance') return {phase:'replay',state:'thinking',action:'watch_new_candles',position:'left',speech:'Появилась новая информация. Пересобери анализ заново, не цепляясь за прошлое решение.',target:'scenario_chart',chartCue:'replay_wait',noHint:true};
    if(event==='replay:complete') return {phase:'replay_complete',state:'celebrate',action:'review_process',position:'right',speech:'Replay завершён. Оцениваем качество решений на каждом этапе, а не то, насколько красиво потом пошла цена.',target:'scenario_chart',chartCue:'replay_complete',noHint:false};
    if(event==='decision:submitted'){
      const ok=!!payload.correct;
      return {phase:'review',state:ok?'correct':'incorrect',action:ok?'inspect_and_acknowledge':'inspect_and_review',position:'right',speech:ok?'Хороший процесс. Теперь посмотрим, что на графике поддерживает это решение.':'Посмотрим на график ещё раз. Важно понять, какой контекст был пропущен.',target:'scenario_chart',chartCue:ok?'review_ok':'review_missed',noHint:false};
    }
    if(event==='timeline:approach') return {phase:'timeline_approach',state:'idle',action:'approach_chart',position:'left',speech:'Сначала посмотрим на текущий контекст.',target:'scenario_chart',chartCue:'focus',noHint:true};
    if(event==='timeline:orient') return {phase:'timeline_orient',state:'thinking',action:'turn_to_chart',position:'left',speech:'Смотри только на то, что уже видно на графике.',target:'scenario_chart',chartCue:'focus',noHint:true};
    if(event==='timeline:present') return {phase:'timeline_present',state:'point',action:'present_task',position:'right',speech:'Обрати внимание на область анализа. Правильный ответ я не показываю.',target:'scenario_chart',chartCue:'focus',noHint:true};
    if(event==='timeline:step_away') return {phase:'timeline_your_turn',state:'thinking',action:'step_away_for_user',position:'left',speech:'Теперь твоя очередь. Я отойду и не буду подсказывать.',target:'scenario_chart',chartCue:'focus',noHint:true};
    if(event==='timeline:return') return {phase:'timeline_return',state:payload.correct===false?'incorrect':'thinking',action:'return_to_chart',position:'right',speech:'Решение зафиксировано. Вернёмся к графику и разберём процесс.',target:'scenario_chart',chartCue:payload.correct===false?'review_missed':'review_ok',noHint:false};
    if(event==='timeline:inspect') return {phase:'timeline_inspect',state:payload.correct===false?'incorrect':'correct',action:'inspect_target',position:'right',speech:'Сравни решение с контекстом, который был доступен в момент выбора.',target:'scenario_chart',chartCue:payload.correct===false?'review_missed':'review_ok',noHint:false};
    if(event==='timeline:explain') return {phase:'timeline_explain',state:payload.correct===false?'incorrect':'correct',action:'explain_review',position:'right',speech:payload.correct===false?'Здесь важна причина ошибки, а не последующее движение цены.':'Хорошо. Закрепи причину решения — не последующий результат цены.',target:'scenario_chart',chartCue:payload.correct===false?'review_missed':'review_ok',noHint:false};
    if(event==='timeline:celebrate') return {phase:'timeline_complete',state:'celebrate',action:'celebrate',position:'left',speech:'Этап завершён. Сохраняем процесс анализа и идём дальше.',target:null,chartCue:null,noHint:false};
    if(event==='lesson:complete') return {phase:'complete',state:'celebrate',action:'celebrate',position:'left',speech:'Урок завершён. Главное — не угадать результат, а повторять правильный процесс анализа.',target:null,chartCue:null,noHint:false};
    return scene;
  }
  function emit(event,payload={}){scene={...scene,...derive(event,payload),event};if(window.MarketChartEventLayer){try{MarketChartEventLayer.sync(scene,event,payload)}catch(err){window.logErr?.('director.sync',err)}}if(window.MarketAICharacterChoreography){try{scene=MarketAICharacterChoreography.resolve(scene,event,payload)}catch(err){window.logErr?.('director.choreo',err)}}listeners.forEach(fn=>{try{fn(scene,event,payload)}catch(err){window.logErr?.('director.listener',err)}});return scene;}
  function get(){return {...scene};}
  function subscribe(fn){listeners.add(fn);return()=>listeners.delete(fn);}
  window.MarketAICharacterDirector={emit,get,subscribe};
})();
